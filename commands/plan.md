# /plan — Универсальная команда планирования

Ты — архитектурный критик проекта. Задача не просто составить план, а найти всё что может пойти не так — до того как написана первая строка кода.

**ЗАПРЕЩЕНО:** писать код, изменять файлы, создавать миграции. Только план, риски, вопросы и checklist.

---

## Рекомендуемая модель

**Strategy:** Default (Sonnet) — основной выбор. Upgrade to Capable (Opus) только при триггерах.

**Default tier (Sonnet):** Основной выбор для всех планов. Достаточна для архитектурного анализа, диагностики, синтеза решений.

**Upgrade to Capable tier (Opus) if:**
- `[contract]` + threat model сложнее стандартного
- `[security]` новый endpoint с нетривиальным threat-моделем
- Multi-service refactor (50+ файлов, много зависимостей)
- Обнаружена системная проблема в диагностике (class-bug в 3+ файлах)
- User explicitly asks for deeper analysis

**❌ Fast tier (Haiku):** НЕ рекомендуется для регулярных планов
- Риск: неправильный синтез, упущение консистентности
- Допускается только для grep/validation если у тебя явно нет reasoning-based шагов
- Лучше переплатить на Default чем пропустить архитектурное нарушение

**Mid-task escalation:** нет (анализ в /plan не требует mid-task переоценки как /code)

**Pre-flight model check:** **да — при старте команды** спроси пользователя какая модель активна (или используй ранее подтверждённую в сессии) и сравни с Default tier. Если mismatch ≥ 2 ступени — пауза + рекомендация перед началом анализа. См. `.claude/model-tiers.md` секция Pre-flight model check.

---

## Навигационная карта шагов

| Шаг | Lite | [code] | [product] | [data] | [security/infra] | [contract] |
|-----|------|--------|-----------|--------|------------------|------------|
| -3 Pre-flight | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| -2 Тип задачи | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| -1 Диагностика | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| -1.5 Верификация | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 0 Вопросы | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| 0.2 Capture | — | — | ✓ | — | — | — |
| 0.3 Карта данных | — | — | — | ✓ | — | — |
| 0.5 Выбор инструмента | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| 0.7 Source Conflict | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| 1 Архитектурный анализ | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| 1.5 Горизонт 3 шага | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| 1.6 Продуктовая проверка | — | — | ✓ | — | — | — |
| 1.7 Контракт | — | — | — | — | — | ✓ |
| 1.8 Threat model | — | — | — | — | ✓ | — |
| 2 Трассировка | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| 3 План | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 4 PRODUCT.md | — | — | ✓ | — | — | — |
| 98 Pre-Mortem | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| 99 Self-Lint | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 100 Финализация | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

Прочитай таблицу ПЕРВЫМ. Пропускай шаги не отмеченные для твоего режима.

---

## Шаг -3: Pre-flight checks (умный гейт)

Каждый триггер — **предложение с подтверждением**, никогда не авто-запуск.

### Подшаг 0 — Проверка незавершённого /code

1. Прочитать `.claude/state/triggers.json`, поле `last_plan_session`
2. Если `last_plan_session.code_run === false`:

```
⚠️ Предыдущий /plan не завершён /code:
   Task:    {task_id}
   Service: {service}
   Started: {completed_at}

Варианты:
  a) Запустить /code для прошлой задачи сначала (рекомендуется)
  b) Отметить как abandoned и продолжить новый /plan
  c) Игнорировать (инкремент skipped_warnings.code_skipped)
```

### Подшаг 1 — Чтение state и инкремент

1. Прочитать `.claude/state/triggers.json`
2. Определить контекст задачи (для multi-service проектов — какой сервис)
3. Инкрементировать счётчики:
   - `last_architecture_audit.plans_since += 1` (если архитектурные триггеры применимы)
   - `last_sync_vision.plans_since += 1` (если есть vision документы)
   - `last_retro.plans_since += 1`
   - `last_product_review.plans_since += 1`
   - `last_product_check.plans_since += 1`
   - `last_product_vision.plans_since += 1`
4. Сохранить triggers.json

### Подшаг 2 — Триггеры (порядок, по одному вопросу за раз)

Каждый вопрос **обязан** включать рекомендуемую модель для предложенной команды (см. `.claude/model-tiers.md` per-command матрица).

**Триггер: /architecture-audit** (если проект имеет архитектурную карту):
- `last_architecture_audit.plans_since` ≥ 5
- → "Архитектурный аудит давно не запускался (N=…). Запустить?
   Рекомендуемая модель: **Default tier** (Sonnet) — типично для audit.
   (y/n/skip)"

**Триггер: /sync-vision** (если есть vision документы):
- `last_sync_vision.plans_since` ≥ 5 И задача меняет контракты/архитектуру
- → "Меняются контракты. Запустить /sync-vision?
   Рекомендуемая модель: **Default tier** (Capable если 10+ inbox И 5+ OQ одновременно).
   (y/n/skip)"

**Триггер: /retro:**
- `last_retro.plans_since` ≥ 15
- → "Накопилось N планов без ретроспективы. Запустить /retro?
   Рекомендуемая модель: **Default tier** (Capable если 60+ DEVLOG entries за период).
   (y/n/skip)"

**Триггер: /product-review** (если есть IDEAS.md):
- Записей в IDEAS.md без `[reviewed]` ≥ 7 ИЛИ `last_product_review.plans_since` ≥ 10
- → "Накопились сигналы (N unreviewed). Запустить /product-review?
   Рекомендуемая модель: **Default tier** (Fast если < 5 IDEAS, Capable если 20+).
   (y/n/skip)"

**Триггер: /product-check** (если есть PRODUCT.md):
- `last_product_check.plans_since` ≥ 5
- → "Изменено N файлов команд. Запустить /product-check?
   Рекомендуемая модель: **Fast tier** (Haiku) — структурное сравнение.
   (y/n/skip)"

**Триггер: USER-MAP sync** (если есть USER-MAP.md):
- `last_user_map_sync.plans_since` ≥ 10 И (добавлена новая команда ИЛИ изменился основной workflow)
- → "USER-MAP может требовать обновления (новая capability или workflow). Проверить и обновить?
   Рекомендуемая модель: **Fast tier** (Haiku) — review existing diagram.
   (y/n/skip)"

**Триггер: /product-vision:**
- `last_product_vision.plans_since` ≥ 30
- → "Стратегическая работа может быть устаревшей. Запустить /product-vision?
   Рекомендуемая модель: **Capable tier** (Opus) — strategic depth обязательна.
   (y/n/skip)"

**Триггер: новая идея без контекста:**
- Запрос описывает фичу не упомянутую в IDEAS.md/ROADMAP.md
- → "Добавить в IDEAS.md перед планированием? (y/n)"

### Подшаг 3 — Stale Open Questions (если есть OPEN-QUESTIONS.md)

Для каждого OQ в области текущей задачи:
- Если status=Open И возраст > 14 дней — показать
- Если истёк `Reminder.Expires` — ⛔ HARD BLOCK до решения PM

### Подшаг 4 — Hard blocks

- Новый сервис/компонент без /architecture-audit → ⛔ требует запуска
- EXPIRED OQ → ⛔ требует решения PM
- Иначе → продолжаем к Шагу -2

---

## Шаг -2 — Тип задачи

| Категория | Описание | Доп. шаги |
|---|---|---|
| `[code]` | Изменение поведения | стандартный набор |
| `[product]` | Изменение UX | + Шаг 1.6 |
| `[data]` | Миграции, схема | + Шаг 0.3 |
| `[infra]` | Серверная конфигурация | + Шаг 1.8 |
| `[security]` | Защита от угроз | + Шаг 1.8 |
| `[process]` | Изменение процесса | упрощённый цикл |
| `[contract]` | API/events | + Шаг 1.7 обязателен |

**Режим Lite vs Full:**
- **Lite**: багфикс ≤ 1 файла < 20 строк → шаги -1 → -1.5 → 3 → 99 → 100
- **Full**: всё остальное → все применимые шаги из навигационной карты

> При сомнении — Full mode.

---

## Шаг -1 — Диагностика

**1. Симптом или причина?**
- Симптом → найди причину, исправь её
- Причина → определи класс проблемы

**2. Код или промпт?**
- Доменное ограничение → ок
- Кейс-ограничение → красный флаг

**3. N-й фикс одной проблемы?**
- grep DEVLOG по тегу за 7 дней
- Если N ≥ 2 → ⛔ СТОП, новая диагностика с реальными данными

---

## Шаг -1.2 — Осознанная полнота решения (Full mode only)

Только для **Full mode**. Lite mode этот шаг пропускает.

Каждое предложенное решение ДОЛЖНО явно указать:

**1. Что закрывается?** (main path, happy path)
- Примеры: "устраняет регрессию в основных сценариях", "фиксит root cause в 80% случаев"

**2. Что НЕ закрывается?** (gaps, edge cases, параллельные пути)
- Явный список: "edge case 1 не покрыт", "параллельный путь A остаётся", "асинхронный сценарий B не тестирован"

**3. Приемлем ли этот gap или требует action?**
- Если gap критичен → "нужны дополнительные шаги: [список]"
- Если gap некритичен → "OK потому что [обоснование]"

⛔ Без этого анализа → план не завершён. Вопрос: "Что мы не делаем и почему это OK?"

---

## Шаг -1.5 — Верификация состояния

- [ ] Прочитан актуальный код функции которую меняешь
- [ ] Проверены логи (если применимо)
- [ ] Проверены данные напрямую (если фикс по данным)
- [ ] grep HYPOTHESES.md по компоненту

**Красные флаги:** "мне кажется" без чтения кода → СТОП

---

## Шаг 0 — Уточняющие вопросы

Задавать только если реально влияет на план. Если всё ясно — переходи к следующему применимому шагу.

---

## Шаг 0.1 — Early pre-mortem (перед planning, не после)

**Принцип:** Real value of pre-mortem до начала planning, на этапе assumptions, не после того как план написан и люди attached к решениям.

Задай 3 вопроса ДО начала архитектурного анализа:

1. **Что мы предполагаем?**
   - Какие состояния внешних сервисов / платформ мы считаем true?
   - Примеры: "Gemini API всегда отвечает", "user всегда авторизован", "кеш актуален"
   - Напиши 3-5 assumptions

2. **Что может пойти не так в первый раз?**
   - Какой сценарий обрушит план на неделю 1?
   - Примеры: "API quota exceeded", "OS case sensitivity", "race condition при одновременных запросах"
   - Напиши 2-3 early failure modes

3. **Какое внешнее состояние может разрушить?**
   - Что я не контролирую и что изменится независимо от моего кода?
   - Примеры: "Gemini history limit", "git network", "database migration lock"
   - Напиши 2-3 external state hazards

**Если любое assumption неверно или не сформулировано** → ⛔ СТОП перед планированием. Переформулируй assumptions чтобы были проверяемы.

---

## Шаг 0.2 — Capture сигнала (только Full + новые фичи)

Если запрос пришёл из боли/friction → добавить в IDEAS.md перед планированием.

---

## Шаг 0.3 — Карта данных и инварианты

1. Какие хранилища затрагивает изменение?
2. Какие инварианты должны сохраниться?
3. Изменение нарушает инварианты? → нужен апдейт data-map
4. Какие данные устареют? Нужна миграция/реиндексация?

---

## Шаг 0.5 — Стратегический выбор инструмента

Перед "как реализовать" — спроси "правильный ли это класс решения?"

- Есть нативный примитив платформы? (Function Calling, schema, type system)
- Не строим ли keyword-матчер вместо использования LLM-native механизма?

**Уровни регулятора (для поведения агентов):**
1. Промпт-правило (игнорируется)
2. Description инструмента (слабо)
3. Few-shot примеры (средне)
4. Структура данных (сильно)
5. Отсутствие альтернативного пути (очень сильно)
6. Schema constraint (гарантия)

Для критичной фичи — уровень 4+ обязателен.

---

## Шаг 0.7 — Source Conflict Check

Запускается если задача интегрирует данные из inbox/чата/внешнего источника.

### Подшаг 1 — Валидация источника

Для каждого факта:
- [ ] Подтверждено в реальном коде? (grep, файлы:строки)
- [ ] Возраст < 30 дней или подтверждено независимым источником?

Если нет — **Type D**, отвергнуть.

### Подшаг 2 — Классификация (5 типов)

| Vision/ADR | Реальность | Тип | Действие |
|---|---|---|---|
| Ничего | Факт есть | **A** | Обновить vision |
| Расплывчато | Факт есть | **E** | Уточнить vision сначала |
| Описывает несделанное | Не реализовано | **B** | RISKS.md / backlog |
| Говорит одно, код другое | Конфликт | **C** | OPEN-QUESTIONS.md |
| Источник невалиден | — | **D** | Архив rejected/ |

---

## Шаг 1 — Архитектурный анализ

- Вписывается ли в текущую архитектуру?
- Какие модули задействованы?
- Не создаём ли дубли существующих модулей/хранилищ?

**Чеклист на рудименты:**
- Похожая логика уже существует?
- Не создаём второе хранилище для тех же данных?
- После изменения — что станет неиспользуемым?

---

## Шаг 1.5 — Горизонт 3 шага вперёд

- Какие задачи из ROADMAP зависят от этого компонента?
- Если код будет вызываться в 10× чаще — что сломается первым?
- Этот компонент — источник правды или производный?

**Связь с VISION:** на какую ось работает задача? Что разблокирует?

---

## Шаг 1.6 — Продуктовая проверка (только [product])

Прочитай IDEAS.md:
1. Закрывает ли фича сигнал из IDEAS.md?
2. Какие соседние боли остаются незакрытыми?

---

## Шаг 1.7 — Контракт с внешним миром (только [contract])

Для каждой внешней зависимости (API, библиотека, сервис):
- [ ] Лимиты (RPM, payload, quota)
- [ ] Что вернётся при превышении / пустом входе?
- [ ] Что при недоступности 5 мин / 1 час?
- [ ] Тариф верифицирован эмпирически (не предположение)?
- [ ] **External state checklist**: какое состояние внешнего сервиса я не могу увидеть?

---

## Шаг 1.8 — Threat model (только [infra] / [security])

Действие пользователя → код → возврат → условие передачи → исключение → финальный получатель

1. Какие реальные угрозы существуют?
2. Какие закрывает решение?
3. Какие остаются?
4. Как УЗНАЕМ что решение работает? (конкретный тест)
5. Что становится сложнее после внедрения?

---

## Шаг 2 — Трассировка пути

Полный путь данных: Действие пользователя → код → возврат → условие передачи → исключение → финальный получатель

**Параллельные пути:** если фиксим X в модуле A — grep по X в других модулях.

**Класс-баги:** при изменении паттерна — grep по паттерну во всём проекте.

---

## Шаг 3 — План реализации

```
## Задача
[Краткое описание]

## Предположения (3-5 ключевых)
- [Предположение 1 которое должно быть истиной]
- [Предположение 2]
Если хоть одно неверно — план не работает.

## Архитектура
- Текущая структура поддерживает фичу?
- Какие модули используются?

## Трассировка пути
[Вход → обработка → получатель + что при исключении]

## Затрагиваемые файлы
- [ ] файл1 — что меняется
- [ ] файл2 — что меняется

## Порядок изменений
1. Сначала ...
2. Затем ...

## Риски
- [риск] → [мера]
  Если игнорировать: [конкретный сценарий поломки]

## Проверка
- Конкретный сценарий: действие → ожидаемый результат

## До / После
**Сейчас:** [текущее поведение]
**После:** [новое поведение]

## Recommended models
**For /code (immediate next):** [tier] — [reasoning из этой конкретной задачи]
**For /review after /code:** [tier]
**For triggered commands** (если предложены в Шаге -3.2):
  - /<command>: [tier] — [typical + override если задача нетипичная]

**Mid-task escalation signals для /code:**
  - [конкретные условия из этой задачи которые сигнализируют upgrade]
```

**Обязательно для блока Recommended models:**
- Tier выбирается из `.claude/model-tiers.md` (per-command матрица).
- Reasoning должен быть про конкретную эту задачу, не generic ("Default для повседневного" — плохо; "Default — 3 файла, без contract — стандарт" — хорошо).
- Если задача нестандартна для команды (например /code на multi-service refactor) — recommended tier для этой задачи может отличаться от типичного для команды.

---

## Шаг 4 — Обновление PRODUCT.md (только [product])

Если изменение меняет поведение:
1. Подготовь текст обновления (бизнес-язык)
2. Покажи: "Предлагаю обновить PRODUCT.md: [текст]"
3. Жди подтверждения

---

## Шаг 98 — Pre-Mortem (Full mode, перед Self-Lint)

⚠️ **Принудительный триггер:** если пользователь выразил сомнение ("точно проверил?") — pre-mortem обязателен в любом режиме.

Представь: прошло 3 месяца, фича работает — но пользователь жалуется. Напиши **6 сценариев**:

1. **Latency/Cost** — стало медленнее / тратит квоту
2. **Повторное использование** — раздражает при регулярном использовании
3. **Edge case в данных** — нестандартные данные ломают
4. **Деградация** — внешний сервис недоступен
5. **Документация** — кто-то читает через 3 месяца — что не поймёт?
6. **Взаимодействие со смежными фичами** — новая фича ломает соседнюю (state pollution, shared cache). Что увидит пользователь в следующих 2-3 запросах?

Для каждого: что сломалось + что в плане это предотвращает.

⛔ Меньше 6 = не выполнен.

---

## Шаг 99 — Self-Lint (ОБЯЗАТЕЛЬНО до показа плана)

✅ Self-lint passed — только после заполнения всех применимых пунктов.

---

## Шаг 99.5 — Модель для /code и /review (ПЕРЕД финализацией)

**Когда план готов к утверждению:**

✅ **Для /code (следующий шаг):**
- Модель: **Default (Sonnet)** (unless upgrade триггер выше в блоке "Рекомендуемая модель")
- Pre-flight check: спроси пользователя какая модель сейчас активна, сравни с рекомендованной
- Если mismatch — рекомендуй переключиться перед /code

✅ **Для /review (после /code):**
- Модель: **Default (Sonnet)** (никогда не ниже Default, даже если /code был на Default)
- Правило: `review_tier = min(/code_tier, Default)` но ≥ Default
- ❌ НЕ использовать Fast (Haiku) для review — риск пропустить консистентность

**Примеры:**
- /code на Default → /review на Default
- /code на Capable → /review на Default
- ❌ /code на Default, /review на Fast — НЕПРАВИЛЬНО

---

## Шаг 100 — Финализация state

После подтверждения плана пользователем:

1. Обновить `last_plan_session`:
```json
{
  "completed_at": "<ISO8601>",
  "task_id": "<краткий ID>",
  "service": "<контекст>",
  "mode": "<Lite|Full>",
  "code_run": false
}
```
2. Сохранить triggers.json
3. Сообщить: "✅ План готов. Следующий шаг: /code на [модель из рекомендации]"

---

⛔ НЕ пиши код. Только план. Жди подтверждения.

$ARGUMENTS
