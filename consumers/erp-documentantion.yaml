# Consumer project reference — erp-documentantion
# v4.34.0+: tokens are NEVER stored in this file or pasted into URLs.
# Use `bash scripts/clone-consumer.sh erp-documentantion` (reads `.env` via
# credential helper — token never appears in shell history or URL).

name: erp-documentantion
repo: https://code.nexchance.de/nexchance/erp-documentantion.git
ssh: git@code.nexchance.de:nexchance/erp-documentantion.git
local_path: consumers/repos/erp-documentantion
purpose: methodology analysis — compare consumer artifacts with methodology templates
status: reference-only  # clone not present until manually cloned

# To clone:
#   bash scripts/clone-consumer.sh erp-documentantion
#
# Prerequisites (one-time setup per developer):
#   GitLab repo (code.nexchance.de) — use GitLab token, NOT GITHUB_PAT.
#   1. Set token: bash scripts/set-secret.sh GITLAB_NEXCHANCE <token>
#   2. Verify:   bash scripts/check-secret.sh GITLAB_NEXCHANCE  # exit 0 = set
#   3. Re-run clone-consumer.sh (configures per-host credential helper
#      automatically via scripts/git-credential-from-env.sh).
#
# To future-pull: cd consumers/repos/erp-documentantion && git pull
