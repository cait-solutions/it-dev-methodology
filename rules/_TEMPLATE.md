# <Stack name> — правила проекта

Технологические правила и инварианты для стека `<stack>`.

Применяется командами `/review` и `/code`. Подсветка нарушений — в sub-agent `qa`.

> Каждое правило должно иметь **обоснование** или ссылку на ADR / RISKS. Правила "потому что так принято" — кандидаты на удаление в `/retro`.

---

## Модели и схема

- <правило 1 — например: Primary key — UUID, не auto-increment integer (ADR-NNN)>
- <правило 2 — например: Каждая таблица имеет tenant_id если данные multi-tenant>
- <правило 3>

## Миграции

- <правило 1 — например: Миграции необратимы в production — продумывай down() только для dev>
- <правило 2 — например: NOT NULL без default на таблице с данными → трёхшаговая миграция>
- <правило 3 — например: Внешние ключи проиндексированы>

## Сервисный слой

- <правило 1 — например: Бизнес-логика — в Service / Action, не в Controller, не в Model>
- <правило 2 — например: Валидация — в Form Request / DTO, не в контроллере>
- <правило 3>

## События и очереди

- <правило 1 — например: Handler идемпотентен — проверяет event_id или business key>
- <правило 2 — например: Job-классы имеют $tries и $backoff>
- <правило 3 — например: failed_jobs таблица в каждом сервисе с очередями>

## Запросы к БД

- <правило 1 — например: Никогда не делай запросы к БД другого компонента напрямую>
- <правило 2 — например: N+1 — использовать eager loading>
- <правило 3 — например: updateOrCreate предпочтительнее firstOrCreate в конкурентных контекстах>

## Тестирование

- <правило 1 — например: Юнит-тесты на бизнес-логику обязательны>
- <правило 2 — например: Интеграционные тесты на handler-ы событий>
- <правило 3 — например: Тест на идемпотентность Job>

## Конфигурация

- <правило 1 — например: Все настройки через env, никаких хардкодов>
- <правило 2 — например: Secrets — в secret manager, не в .env коммитятся>
- <правило 3>

## Стек-специфичные анти-паттерны

Что флагать как 🔴 CRITICAL в `/review`:

- <анти-паттерн 1>
- <анти-паттерн 2>
- <анти-паттерн 3>
