# SKILL — <Domain Name>

Domain-specific skill / knowledge document. Создаётся через `/onboard` при передаче legacy домена под AI-агента.

> **Цель:** дать AI-агенту достаточно контекста чтобы безопасно работать с доменом без чтения всего кода. Источник — **реальный код**, не vision.

---

## Что делает этот домен

<1 абзац: какую бизнес-задачу решает домен, какие сценарии покрывает.>

---

## Источник правды

Этот домен является источником правды для следующих данных:

- <Сущность / таблица 1>
- <Сущность / таблица 2>

Эти данные используются другими доменами через:
- API: <endpoint group>
- События: <event names / exchange>

---

## События

### Publisher

Какие события домен публикует:

| Событие | Когда срабатывает | Subscribers |
|---|---|---|
| `<event.name>` | <условие> | <known subscribers> |

### Subscriber

Какие события домен слушает:

| Событие | От кого | Что делает |
|---|---|---|
| `<event.name>` | <publisher> | <обработка> |

---

## Внешние интеграции

| Сервис | Назначение | Тип | Документация |
|---|---|---|---|
| | | HTTP / events / SDK | <link> |

---

## Точки входа

Основные API / роуты / интерфейсы домена.

- `<HTTP method> /<route>` — <назначение>
- `<command / cli>` — <назначение>

---

## Внутренняя архитектура

<Кратко — основные модули и их роли. Не дублируй SYSTEM-MAP.>

- `<path/to/module>` — <роль>
- `<path/to/module>` — <роль>

---

## Известные ограничения и техдолг

Что важно знать AI-агенту чтобы не "исправить" как баг:

- <ограничение 1 — почему именно так>
- <известный gap — ссылка на RISKS.md R-NN>

---

## Risk map для AI-агента

Зоны где агент может навредить — требуют явного approve владельца:

### ⚠️ Требует явного approve

- <операция 1 — например "Удаление сущности X — каскадно затрагивает Y, Z">
- <миграция 2 — например "ALTER TABLE с не-NULL колонкой">

### ⛔ Запрещено без архитектора

- <операция — например "Изменение event schema">
- <операция — например "Переименование таблицы">

### ✅ Безопасно автономно

- <операция — например "Создание новой read-only endpoint">
- <операция — например "Добавление индекса">

---

## Параллельные пути в коде

Если в домене есть аналогичные паттерны в нескольких местах — перечисли. Это критично для класс-багов (фиксим в одном месте — проверь все).

- <паттерн 1 в файле A> ≈ <паттерн в файле B>: <как искать grep'ом>

---

## Контакты

- **Владелец:** <команда / человек>
- **Контрибьюторы:** <если применимо>
- **Контакт для вопросов:** <channel / email>
