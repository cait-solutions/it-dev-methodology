#!/usr/bin/env bash
# _fixture_pair.sh — VERSION A (canonic copy для parity-divergent sandbox)
# Намеренно отличается от templates/scripts/_fixture_pair.sh на одну строку.
# Используется test-validators.sh для доказательства что validate-script-parity.sh
# обнаруживает расхождение (exit 1).
echo "fixture-version-A"
