#!/usr/bin/env bash
# _fixture_pair.sh — VERSION B (template copy для parity-divergent sandbox)
# Намеренно отличается от scripts/_fixture_pair.sh на одну строку (drift).
# Используется test-validators.sh для доказательства что validate-script-parity.sh
# обнаруживает расхождение (exit 1).
echo "fixture-version-B"
